import { createStore } from 'vuex'
import data from '@/data'

export default createStore({
  state: {
    ...data,
    authUserId: ''
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
